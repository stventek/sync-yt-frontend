import { createTheme } from "@material-ui/core";

const theme = createTheme({palette:  {
    type: 'dark',
    primary: {
        main: '#000'
    },
    secondary: {
        main: '#ee0f0f'
    }
}});

export default theme;