import { createTheme } from "@material-ui/core";

const darkTheme = createTheme({palette:  {
    type: 'dark',
    primary: {
        main: '#000'
    },
    secondary: {
        main: '#ee0f0f'
    }
}});

export default darkTheme;